# Сборка preview APK / EAS profile `preview` — тот же staging API, что в `.env.staging.example`.
# Скопируй в `.env` перед `npm run build:apk` или используй профиль EAS `preview`.
#
# Staging origin (Railway):
EXPO_PUBLIC_API_URL=https://backend-production-2829.up.railway.app
EXPO_PUBLIC_DEV_BYPASS_AUTH=false
EXPO_PUBLIC_BIOMETRICS_ONLY=true
