/**
 * Целевые значения числовых метрик — не медицинские нормы; BA меняет после калибровки на ваших данных.
 * Фактические шкалы см. комментарии к формулам в `compute-pose-numeric-rows.ts`.
 *
 * Принцип отбора: каждая метрика должна быть **анатомически интерпретируемой**, а не зависеть от
 * того, насколько близко стоит пользователь и как он попадает в кадр. Поэтому метрики вида
 * «(1 − Y_плеча) × 100» и «| midShoulder − midHip | × 100» сюда не входят: это композиция кадра,
 * а не осанка.
 */
export const POSE_NUMERIC_IDEALS = {
  /** Анфас: разница Y между плечами × 100 (магнитуда). ~0 = плечи на одной высоте. */
  frontal_shoulder_dy_asymmetry: 0,
  /** Анфас: магнитуда угла линии плеч к горизонтали кадра, ° (всегда 0…90). */
  frontal_shoulder_line_tilt_deg: 0,
  /** Анфас: разница Y между точками таза × 100 (магнитуда). */
  frontal_hip_dy_asymmetry: 0,
  /** Анфас: магнитуда угла «нос → середина плеч» к вертикали кадра, ° — наклон головы вбок. */
  frontal_head_tilt_deg: 0,
  /** Анфас: магнитуда угла «середина плеч → середина таза» к вертикали кадра, ° — наклон корпуса вбок. */
  frontal_trunk_lean_deg: 0,
  /** Анфас: |наклон линии плеч − наклон линии таза|, ° — выявляет перекос таза относительно плеч. */
  frontal_shoulder_hip_parallelism_deg: 0,
  frontal_min_visibility: 1,

  /**
   * Профиль: |Δx| между точкой уха/носа и плечом × 100 (только горизонтальная компонента — это и есть
   * «голова вперёд» FHP). Полный hypot включал бы вертикальный зазор «голова над плечом», который у
   * любого здорового человека ~10–15: тогда идеал 0 недостижим в принципе. Берём только горизонталь.
   */
  profile_ear_forward_shift: 0,
  /**
   * Профиль: внутренний угол ° в точке плеча между линиями «середина таза–плечо» и «плечо–ухо».
   * 180° = голова полностью «над» плечом и плечом «над» тазом (идеальная стопка). Норма 165–175°.
   */
  profile_shoulder_cervico_thoracic_angle: 170,
  /** Профиль: магнитуда угла линии «плечо–ухо/нос» к вертикали кадра, ° — наклон шеи вперёд. */
  profile_head_stack_angle: 0,
  /** Профиль: магнитуда угла линии «середина таза → плечо» к вертикали кадра, °. */
  profile_trunk_lean_deg: 0,
  profile_min_visibility: 1,
} as const;
