{
  "name": "neurosync",
  "private": true,
  "scripts": {
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix admin && npm install --prefix mobile",
    "lint": "npm run lint --prefix backend && npm run lint --prefix admin",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,yml,yaml}\""
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  },
  "engines": {
    "node": ">=20"
  }
}
